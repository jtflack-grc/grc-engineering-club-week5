# Week 5 Evidence

The original AWS-native evidence was captured locally from CloudTrail and Security Hub, then summarized before publication.

Raw Security Hub findings and Terraform outputs were not committed because they include AWS account IDs, ARNs, resource names, subnet IDs, security group IDs, IAM role names, and workload tags.

Public evidence retained here:
- `security-hub-findings-summary.json`
- `cloudtrail-status-summary.json`

These preserve the portfolio proof without publishing detailed account reconnaissance data.
